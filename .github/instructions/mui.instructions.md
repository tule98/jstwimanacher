---
applyTo: "**"
---

<rules>
- spacing: prefer using mui's spacing utility over hardcoded values
- variants: prefer using mui's typography variants over custom styles
- colors: prefer using mui's theme palette colors over hardcoded color values
</rules>

<animation>
- prefer using mui's built in transition components
</animation>
